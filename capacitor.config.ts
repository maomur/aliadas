import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aliadas.test',
  appName: 'aliadas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
